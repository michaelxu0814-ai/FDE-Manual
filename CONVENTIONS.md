# 工作规约 — 人与 AI 的分工卡点

> 本文件是"动作级"的约定：谁在什么时候必须做什么。跟 README（学什么）、
> week-0X-tasks.md（今天做什么）不同，这里管的是**怎么做才算合规**。
> 每一条都必须绑定到一个具体动作上——没有绑定动作的纪律等于不存在。

## 为什么有这份文件

2026-08-18 Day 1 暴露的问题：任务表里写了"装包前要注意来源安全"，但实际装
`anthropic` 时，Claude 凭已有知识直接装了，用户也没验证，**没有任何一方真的执行过这条
纪律**。纪律写在文档里、却没绑到具体动作上，就是一句空话。

FDE 的工作里这类"说了但没落到动作上"的漏洞是要出事故的。所以从这里开始，
凡是有风险的动作，都要有明确的卡点：**谁做、什么时候做、不做会怎样。**

---

## 卡点 1 · 安装任何新包之前

**触发时机**：任何 `pip install <包名>` 执行**之前**（不是之后）。

**为什么必须在之前**：PyPI 上的包在安装过程中就可以执行代码。等装完再检查，恶意代码
已经跑过了。这条不能商量。

**Claude 必须做的**（每一个包，无一例外）：

在执行安装命令前，先给出这张卡：

| 项 | 内容 |
|---|---|
| 包名 | |
| 它是什么、这次为什么要装它 | |
| 谁维护的 | 官方组织 / 知名开源项目 / 个人项目 / 不确定 |
| 判断依据 | 来自官方文档？还是我的既有知识？**必须说清哪一种** |
| 风险等级 | 低（官方且广泛使用）/ 中（第三方但主流）/ 高（冷门、名字近似知名包、来源不清） |
| 会连带装进来哪些依赖 | 至少说明大致范围 |

然后**停下来等用户点头**，不要在同一步里把安装也做了。

**风险等级为"中"或"高"时**，Claude 还必须实际去查证，不能只凭记忆：查 pypi.org 上的
维护者、发布历史、项目主页，把查到的结果给用户看，再等确认。

**用户必须做的**：看完卡再决定。不用懂技术细节，重点看三件事——
1. **判断依据那栏写的是"官方文档"还是"我的既有知识"**——后者意味着没人真的去查过
2. 包名跟你预期的是否**完全一致**（`anthropic` vs `anthropics`、`requests` vs `request`，
   差一个字母就是典型的 typosquatting 攻击）
3. 风险等级是中或高时，有没有附上实际查证的结果

**例外**：同一个项目里重复安装已经过卡的包（比如换台机器重建环境、`pip install -r
requirements.txt` 装的是之前已经逐个过过卡的清单）不必重复走流程。

---

## 卡点 2 · 任何密钥、token、密码进入文件之前

**触发时机**：要把 API key 之类的东西写进任何文件之前。

**Claude 必须做的**：
- 绝不把密钥明文写进代码文件
- 写进 `.env` 之前，先确认 `.gitignore` 里已经有 `.env`——**顺序不能反**
- 每次 `git add` / `git commit` 前，检查这次要提交的文件里有没有密钥模样的字符串

**用户必须做的**：申请 key 时不要贴到聊天框里给 Claude 看。Claude 不需要知道 key 的值，
只需要知道"它存在环境变量 `ANTHROPIC_API_KEY` 里"。

**万一泄露了怎么办**（要提前知道，不是出事再学）：立刻去控制台把那个 key 作废
（revoke）、重新生成一个。不要指望"删掉那次提交就没事了"——Git 历史里还在，公开仓库
被爬走只需要几分钟。

---

## 卡点 3 · 执行前必须解释

**触发时机**：Claude 要执行任何用户看不懂的命令之前。

**Claude 必须做的**：先说这一步在做什么、为什么，再执行。不允许"一口气跑完再统一解释"——
那样用户就退化成复制粘贴选手了，这正是 Week 1 任务表反复强调要避免的。

**用户必须做的**：看不懂就问，不要让它过。**"能跑就行"是这个学习计划里最需要警惕的状态。**

---

## 这份规约本身的用法

这些规则要在**用户自己电脑上的项目里也生效**，而不只在这个手册仓库里。做法是在项目
文件夹里放一个 `CLAUDE.md`，Claude Code 每次在那个项目里工作时会自动读它。

具体怎么放，见 `automation/local-claude-md-template.md`。
